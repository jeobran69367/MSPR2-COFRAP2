# Dépendances de développement / CI (tests, lint, sécurité)
# Les dépendances applicatives vivent dans functions/*/requirements.txt
# et frontend/requirements.txt — cf. README pour l'installation complète.

pytest==8.2.2
pytest-cov==5.0.0
ruff==0.5.0
bandit==1.7.9
httpx==0.27.0
